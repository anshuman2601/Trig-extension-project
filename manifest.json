{
  "manifest_version": 2,

  "name": "Trig Plugin",
  "description": "This plugin will analyze a webpage to search for trigger words harmful for fragile individuals",
  "version": "1.0",

  "browser_action": {
   "default_icon": "icon.png",
   "default_popup": "popup.html"
  },
  "permissions": [
   "activeTab"
   ]

"background_page": "background.html",
"permission": [ "tabs" ],
if (condition == true) {
chrome.tabs.create({url:"popup.html"});
}
}